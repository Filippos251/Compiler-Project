public class Sample {

    public float rate;                   // 'float' is not a defined data type

    private int 5count = 10;            // Variable name cannot start with a digit

    public void compute() {
        x == 5;                         // Misuse of '==' instead of '=' in assignment

        if (x <> 10) {                  //  '<>' is not a valid comparison operator
            out.prnt("Wrong call", x);  //  'prnt' is not 'print'; function not defined
        } else
            print("x is", x);           //  Direct call to print without 'out.' object
    }

    public unknownType method() {       // 'unknownType' is not defined in grammar
        return true;                    // return type is invalid
    }

    public void loopTest() {
        for (i = 0; i < 10; i++)        // Missing body braces for for-loop
            x = x + 1
    }                                   // Missing semicolon after assignment

    private double calc() {
        int val = ;                     // Incomplete assignment
        do {                            
            val = val + 1              //  Missing semicolon
        } while (val < 10              // Missing closing parenthesis and semicolon
    }
}
